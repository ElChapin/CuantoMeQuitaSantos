'use strict';

describe('myApp.paso1 module', function() {

  beforeEach(module('myApp.paso1'));

  describe('paso1 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var paso1Ctrl = $controller('Paso1Ctrl');
      expect(paso1Ctrl).toBeDefined();
    }));

  });
});